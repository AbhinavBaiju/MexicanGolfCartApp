{
    "name": "mexican-golf-carts-app",
    "version": "0.0.0",
    "private": true,
    "workspaces": [
        "apps/*",
        "worker",
        "shared"
    ],
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "concurrently": "^8.0.0"
    }
}